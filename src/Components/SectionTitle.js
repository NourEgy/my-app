const SectionTitle = (props) => {
    return (
        <div className="section__section-title">
              <p>{props.subtitle}</p>   
            <h2>{props.title}</h2>
          
      </div>
    )
}

export default SectionTitle;